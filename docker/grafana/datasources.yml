apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

dashboards:
  - name: 'Agentic Control Tower'
    folder: 'AgenticOS'
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards